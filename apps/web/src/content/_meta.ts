import type { MetaRecord } from 'nextra';

const meta: MetaRecord = {
  index: 'Introduction',
  'edge-sdk': 'Edge SDK',
  'leap-bundle': 'Model Bundling Service',
  lfm2: 'LFM2',
  changelog: 'Changelog',
};

export default meta;
